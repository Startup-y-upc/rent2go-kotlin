# 2026-07-06: no secrets currently required. The app used to depend on
# MAPS_API_KEY (Google Maps Platform) but has migrated to OSMDroid
# (OpenStreetMap), which does not require an API key. This file is kept as a
# placeholder in case a future secret (e.g. a different provider's token) is
# needed; the secrets-gradle-plugin dependency was removed from build.gradle.kts.
